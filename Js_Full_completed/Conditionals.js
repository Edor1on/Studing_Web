const isUserLoggedIn = true;

// If Else operator

if (isUserLoggedIn) {
  alert("You are logged in!");
} else {
  alert("You are not logged in!");
}

// Ternary operator

// isUserLoggedIn ? alert("You are logged in!") : alert("You are not logged in!");

// Switch operator

const age = 30;

switch (age) {
    case 10:
        alert("Happy birthday, give me a kiss!");
    break;
    case 15:
        alert("Happy birthday, give me a kiss!");
    break;
    case 30:
        alert("Kiss me, thirty-year-old MILF!");
    break;
    default:
        alert("You are not a special age!");
    break;
}
