
### 🏗️ 1. Для Батька (Grid Container)

**`display: grid;`** — Вмикає режим сітки (елемент стає блоковим).
**`display: inline-grid;`** — Вмикає сітку, але сам елемент поводиться як рядок (рідко треба).

#### 📐 Розмітка сітки (Шаблон)
* **`grid-template-columns`** — Ширина колонок (вертикальні стовпці).
    * `100px 200px` — дві колонки фіксованої ширини.
    * `1fr 2fr` — пропорції (одна частина і дві частини вільного місця).
    * `repeat(3, 1fr)` — створити 3 однакові колонки (скорочення).
    * `auto` — ширина по контенту.
* **`grid-template-rows`** — Висота рядків (горизонтальні смуги).
    * Працює так само, як і columns.
* **`grid-template`** — Шорткат: `рядки / колонки`.
    * Приклад: `1fr 50px / 20% 80%`.

#### ↔️ Відступи (Gaps)
* **`gap`** — Відступ між усіма клітинками.
* **`row-gap`** — Тільки між рядками.
* **`column-gap`** — Тільки між колонками.

#### 🎯 Вирівнювання (Всередині клітинки)
* **`justify-items`** (Горизонтально: вліво/вправо в межах клітинки).
    * `start` — зліва.
    * `end` — справа.
    * `center` — по центру.
    * `stretch` — розтягнути на всю ширину (стандарт).
* **`align-items`** (Вертикально: вверх/вниз в межах клітинки).
    * `start` — зверху.
    * `end` — знизу.
    * `center` — по центру.
    * `stretch` — розтягнути на всю висоту (стандарт).

#### 📏 Вирівнювання (Всієї сітки всередині контейнера)
*(Тільки якщо сітка менша за сам контейнер)*
* **`justify-content`** (Горизонтально).
    * `start`, `end`, `center`, `space-between`, `space-around`, `space-evenly`.
* **`align-content`** (Вертикально).
    * Ті самі значення.

---

### 👶 2. Для Дітей (Grid Items)

#### 📍 Розміщення (Де жити?)
* **`grid-column-start`** / **`grid-column-end`** — З якої лінії почати і на якій закінчити.
* **`grid-column`** — Скорочення: `start / end`.
    * `1 / 3` — від 1-ї лінії до 3-ї (займе 2 клітинки).
    * `2 / -1` — від 2-ї лінії до самого кінця.
    * `span 2` — розтягнутися на 2 клітинки від поточної позиції.
* **`grid-row`** — Те саме для рядків (вертикально).
    * `1 / 2` — зайняти перший рядок.

#### 🎨 Індивідуальне вирівнювання (Перебиває батька)
* **`justify-self`** — Вирівняти ТІЛЬКИ ЦЕЙ елемент горизонтально (`start`, `center`, `end`, `stretch`).
* **`align-self`** — Вирівняти ТІЛЬКИ ЦЕЙ елемент вертикально.

---

### ⚡ Корисні одиниці та функції

* **`fr` (fraction)** — "Частка" вільного простору. Найкраща одиниця для адаптивних сіток.
    * `1fr 1fr` — дві рівні половини.
    * `1fr 3fr` — перший 25%, другий 75%.
* **`repeat(кількість, розмір)`** — Щоб не писати `1fr 1fr 1fr 1fr`.
    * `repeat(4, 1fr)`.
* **`minmax(min, max)`** — "Не менше ніж X, але не більше ніж Y".
    * `minmax(200px, 1fr)` — колонка буде мінімум 200px, але може розтягуватись.

Зберігай це собі. Тепер ти знаєш базу обох систем верстки!